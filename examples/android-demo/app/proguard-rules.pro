# Add project specific ProGuard rules here.
# You can control the set of applied configuration files using the
# proguardFiles setting in build.gradle.

# Keep application specific classes (prevent application layer classes from being obfuscated to avoid debugging difficulties)
-keep class com.plaud.nicebuild.** { *; } 